# Bash
alias ll='ls -lG'
alias la='ls -laG'

# Ruby on Rails
alias migrate='bundle exec rake db:migrate RAILS_ENV=development & bundle exec rake db:migrate RAILS_ENV=test'
alias rollback='bundle exec rake db:rollback RAILS_ENV=development & bundle exec rake db:rollback RAILS_ENV=test'
alias guard='guard --no-bundler-warning'

# Sublime Text
alias subl="project_aware_subl"